# Database
DATABASE_URL=postgresql://webmux:webmux@db:5432/webmux?schema=public
POSTGRES_PASSWORD=webmux

# Auth (leave empty to disable password)
WEBMUX_PASSWORD=
WEBMUX_SECRET=

# Production (Linux) — match host dev user for tmux socket access
# Run: id -u && id -g to find these values
HOST_UID=1002
HOST_GID=1003
HOST_USER_HOME=/home/devops_bot
VIRTUAL_HOST=webmux.pangogo.com
LETSENCRYPT_EMAIL=audilu@panmedia.asia
