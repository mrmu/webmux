# webmux

AI-Ops development platform — web-based tmux session manager with Claude Code integration.

## 本機部署

我在 /etc/hosts 設定好本機的網域為 webmux.test，不要用localhost:3000。
每次修改都要先測試，前後端都要，然後要確認有把最新進度更新到容器，然後確認容器有運行正常。
